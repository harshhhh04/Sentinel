#pragma once

#include <cstdint>

// Time
using Timestamp = uint64_t;

// Identifiers
using ObservationID = uint32_t;
using EventID       = uint32_t;
using SourceID      = uint16_t;

// Generic value type
using Value = float;